---
description: Clean the project
---

# /clean
1. Execute the shell command: `bash scripts/clean.sh` 
2. If the script returns a non-zero exit code or does not return "Temp folder is now empty." as the last line, *STOP* and report the status to the user.