# [COMPLETED] Task-0002: Refactor worker-bridge.sh for Robust Asset Resolution

## 🎯 Context & Objective
- **Problem:** `worker-bridge.sh` fails when the repository is located in nested directory structures (e.g., `~/Project/{identity}/{project-name}`) because it uses fragile relative path calculations to find core worker assets.
- **Goal:** Update the script to use `${HOME}/.agents/core-worker` as the primary source for core assets, ensuring robustness across different installation paths.
- **Reference:** [ADR-001](file:///home/mike/Projects/mike-heckman/ai-project-configuration/docs/architecture-decisions/ADR-001.md)

---

## 🛠 Implementation Plan
> [!NOTE]
> This section is for the Architect to define the "How" and for the Coder to follow.

### Proposed Changes
#### [MODIFY] [worker-bridge.sh](file:///home/mike/Projects/mike-heckman/ai-project-configuration/scripts/worker-bridge.sh)
- [x] Update `CORE_WORKER_PATH` to prioritize `${HOME}/.agents/core-worker`.
- [x] Update `ENV_TEMPLATE` to use the new `CORE_WORKER_PATH`.
- [x] Add a check to ensure `CORE_WORKER_PATH` exists before proceeding.

#### [MODIFY] Project-wide references
- [x] Replace all occurrences of `jcodemunch` with `jcodemunch-mcp`.
- [x] Replace all occurrences of `jdocmunch` with `jdocmunch-mcp`.
- [x] Update documentation and rules to reflect the new naming convention.

---

## ✅ Success Criteria (Definition of Done)
> [!IMPORTANT]
> The Coder must satisfy all criteria before handing off to the Reviewer.

### Verification Steps
- [x] `./scripts/worker-bridge.sh . coder python` (should not fail with "Missing source path" or "can't read" errors).
- [ ] `./scripts/lint.sh`

### Expected Behavior
- The script correctly identifies the `core-worker` directory via the `~/.agents` link.
- The Incus VM launches and mounts the `core-worker` volume successfully.
- The `.agent-worker-env` file is generated correctly using the template from the linked directory.

---

## 🏁 Completion Status
- **Reviewer Approval:** [x]
- **Completion Date:** 2026-04-27
